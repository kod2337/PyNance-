 Finance Tracker Environment Configuration Template
# Copy these environment variables to your system or create a .env file

# Google Sheets Configuration
FINANCE_TRACKER_CREDENTIALS=credentials.json
FINANCE_TRACKER_SPREADSHEET=Finance Tracker Automated
FINANCE_TRACKER_EMAIL=your-email@gmail.com

# Security Settings (Optional - uses defaults if not set)
MAX_TRANSACTION_AMOUNT=1000000
MAX_DESCRIPTION_LENGTH=200
MAX_CATEGORY_LENGTH=50

# API Settings (Optional - uses defaults if not set)
MAX_API_RETRIES=3
API_RETRY_DELAY=1.0

# Google Sheets API Configuration
GOOGLE_APPLICATION_CREDENTIALS=credentials.json
SPREADSHEET_NAME=Finance Tracker Automated
USER_EMAIL=your-email@gmail.com

# Google Gemini AI Configuration (for AI features)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Gemini Model Configuration (optional)
# Available models: gemini-2.0-flash-thinking-exp, gemini-1.5-flash, gemini-1.5-pro
GEMINI_MODEL=gemini-2.0-flash-thinking-exp

# Application Settings
AUTO_UPDATE_CHARTS=true
MAX_RECENT_TRANSACTIONS=20
DEFAULT_CURRENCY=USD
DEFAULT_CHART_TYPE=line

# Database Configuration (if using local database)
DATABASE_URL=sqlite:///finance_tracker.db

# Logging Configuration
LOG_LEVEL=INFO
LOG_FILE=logs/finance_tracker.log

# Chart Configuration
CHART_WIDTH=800
CHART_HEIGHT=600
CHART_COLORS=#4285F4,#EA4335,#FBBC05,#34A853,#9AA0A6

# Instructions:
# 1. Set these as environment variables in your system, OR
# 2. Copy this content to a '.env' file in the same directory
# 3. Update the values above with your actual configuration
# 4. Never commit credential files or .env files to version control 